def my_algorithm(arr):
    """
    Находит максимальную сумму непрерывного подмассива 
    с использованием алгоритма Кадане.

    Алгоритм:
    1. Инициализировать текущую сумму (current_sum) и максимальную сумму (max_sum)
       первым элементом массива.
    2. Для каждого следующего элемента массива:
       a) вычислить новую текущую сумму как максимум между:
          - текущим элементом (начало нового подмассива)
          - суммой текущего элемента и предыдущей текущей суммы 
            (продолжение текущего подмассива)
       b) обновить максимальную сумму, если текущая сумма стала больше.
    3. После завершения прохода вернуть максимальную найденную сумму.

    Входные данные:
    arr: list[int] — список целых чисел.

    Ограничения:
    - массив может быть пустым (в этом случае возвращается 0)
    - может содержать как положительные, так и отрицательные числа
    - длина массива не ограничена

    Выходные данные:
    int — максимальная сумма непрерывного подмассива.

    Сложность:
    Временная: O(n) — один проход по массиву
    Пространственная: O(1) — используются только две дополнительные переменные

    Пример:
    >>> my_algorithm([1, 2, 3, -2, 5])
    9
    >>> my_algorithm([-1, -2, -3, -4])
    -1
    >>> my_algorithm([5, 4, -1, 7, 8])
    23
    >>> my_algorithm([])
    0
    """
    pass